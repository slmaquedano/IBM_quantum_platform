	#Abre a pasta do repositório:

git init
git branch -M main
git remote add origin https://github.com/slmaquedano/IBM_quantum_platform.git 
	# Gerar esse código no site do git na pasta do repositório (botão verde superior direito)
git pull origin main --allow-unrelated-histories
git add .
git commit -m "initial commit"
git push -u origin main

	#Para atualizar 
git add nome_do_arquivo
git commit -m "comentario_"
git push